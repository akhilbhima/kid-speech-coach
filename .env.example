# Anthropic API key — the kid-GPA makes one Claude call per speech.
# Get one at https://console.anthropic.com/ . Set this in Vercel project settings (not committed).
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# KID_SPEECH_MODEL=claude-sonnet-4-6
